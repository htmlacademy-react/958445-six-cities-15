export enum AuthorizationStatusesEnum {
  AUTH = 'AUTH',
  NO_AUTH = 'NO_AUTH',
}

export enum AppRoutesEnum {
  HOME = '/',
  ROUTE_STAR = '*',
  LOGIN = '/login',
  OFFER = '/offer',
  FAVORITES = '/favorites',
}

export enum SortTypesEnum {
  POPULAR = 0,
  PRICE_TO_HIGH = 1,
  PRICE_TO_LOW = 2,
  RATED_FIRST = 3,
}
