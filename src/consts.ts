export enum AuthorizationStatusesEnum {
  AUTH = 'AUTH',
  NO_AUTH = 'NO_AUTH',
}

export enum AppRoutesEnum {
  HOME = '/',
  ROUTE_STAR = '*',
  LOGIN = '/login',
  OFFER = '/offer',
  FAVORITES = '/favorites',
}
